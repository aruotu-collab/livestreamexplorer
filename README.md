# LiveStream Explorer

Buyer-intelligence platform for livestream commerce. The calendar is free. The product is knowing which streams are worth your time.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
